export interface Donut {
    id: string;
    ref: string;
    calories: string;
    extras: string[];
    photo: string;
}

export interface Donuts {
    results: string[];
}

export interface Results {
    id: string;
    ref: string;
}
